# Alba — /usr/lib/ostree/prepare-root.conf
# Valeurs prouvées relevées sur Aurora (bootc KDE, Fedora 44).
# Active la racine composefs (immuable + vérifiable) et le sysroot en lecture seule.
[composefs]
enabled = yes

[sysroot]
readonly = true
