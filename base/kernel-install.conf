# Alba — /usr/lib/kernel/install.conf
# Copié du jeu baseimage upstream de bootc.
# kernel-install ne lance PAS dracut lui-même : ostree/bootc gèrent le placement
# du noyau et de l'initramfs dans /usr/lib/modules/$kver/ (layout ostree).
layout=ostree
